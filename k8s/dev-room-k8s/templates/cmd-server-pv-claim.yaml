apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Values.cmd_server_name }}-pv-claim
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: {{ .Values.pv_capacity }}
  storageClassName: ""